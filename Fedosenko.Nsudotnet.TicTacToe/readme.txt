Commands:
    help - this text in game;
    set - set symbol on field in position bigFieldX bigFieldY smallFieldX smallFieldY (0 <= for all <= 2). If you locked on field just type "set smallFieldX smallFieldY";
    exit - close game;
    new - start new game.
